"""This module was created by django when the app was created. It
defines a ``GithubConfig`` class that can be used for registering this
app in the settings file.
"""

from django.apps import AppConfig


class GithubConfig(AppConfig):
    name = 'github'

"""This module was created by django when the app was created.
It defines an ``UpdateConfig`` class that can be used for registering
this app in the settings file.

"""

from django.apps import AppConfig

class UpdateConfig(AppConfig):
    name = 'update'
